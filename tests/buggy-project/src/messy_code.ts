export function calc(a: number, b: number) {
    // LOW: Magic number 86400, redundant logic
    const res = a * 86400;
    const result = res;
    if (result === res) {
        return result;
    }
    return res;
}
